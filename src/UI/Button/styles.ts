import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        padding: 20,
        backgroundColor: 'rgb(232 237 234)',
        borderRadius: 10,
        borderColor: 'black',
        borderWidth: 1
    },
    title: {
        fontSize: 15
    }
});