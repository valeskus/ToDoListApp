import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: 150,
        alignItems: 'center',
        justifyContent: 'space-around',
        flexDirection: 'row',
        paddingTop: 20
    },
    title: {
        fontSize: 25,
        fontWeight: '500'
    }

});